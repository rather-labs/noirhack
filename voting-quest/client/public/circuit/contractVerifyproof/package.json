{
  "name": "jwtvoting",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "rm -rf ./dest && tsc -b",
    "deploy": "npm run build && node --experimental-specifier-resolution=node ./dest/deploy.js"
  },
  "dependencies": {
    "@aztec/accounts": "0.84.0",
    "@aztec/aztec.js": "0.84.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
